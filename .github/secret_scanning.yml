# Exclude demo fixture files from GitHub secret scanning.
# All values in demo/ are intentionally fake — they exist to show ContextDuty
# catching them. They are not real credentials.
paths-ignore:
  - "demo/**"
